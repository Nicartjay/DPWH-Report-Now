// Minimal Next.js App component
// This is required for Next.js but we're not using React for the main app

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

// Made with Bob
